-Agregé un map $color en variables.scss. Llamé a $color con map-get en normalize.scss, en footer.scss y en header.scss
-Creé un mixin sencillo y lo apliqué en los diferentes archivos .scss
-Apliqué un extend en la página de inicio.scss, cuando defino la clase .gallery
-Agregué keywords, description y pongo mas info a los <título> de cada página.